git cmd = command
1. git status (finding changes)
2. git add filename, .,* (.=gitignore, *= all)
3. git commit -m "comment"
If new account
 (i) git config --global user.name "user name"
 (ii) git config --global user.eamil "user email"
4. git push -u origin main
fstdhryuj